/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the dev-tools badge so it doesn't overlap the prototype during tests.
  devIndicators: false,
};

export default nextConfig;
